{
  "scripts": {
    "build": "node scripts/build-question-bank.js",
    "start": "npm run build && wrangler dev",
    "dev": "wrangler dev",
    "deploy": "npm run build && wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply onthi-lop6-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply onthi-lop6-db --remote"
  },
  "dependencies": {
    "pdf-parse": "^2.4.5"
  },
  "devDependencies": {
    "wrangler": "^4.78.0"
  }
}
